# Runtime logs and Windows Explorer metadata
*.log
Thumbs.db
Desktop.ini

# The Spotify plugin is intentionally self-contained; keep its bundled
# plugin/node_modules directory tracked so Fifine can load it directly.
